class A{
        constructor(name , age){
                this.name = name;
                this.age = age;
        }
        login(){
                console.log(`${this.name} is logged in`);
        }
}